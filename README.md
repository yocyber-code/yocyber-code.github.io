# yocyber-code.github.io

Personal portfolio — Watchara Sueasakul (Yoshi). Next.js 15 (App Router) static export (SSG), deployed to GitHub Pages via Actions.

```bash
pnpm install
pnpm dev      # local dev
pnpm build    # static export to ./out
```
